[Skip to main content](https://developers.google.com/admob/android/native#main-content)

- [GoogleAdMob](https://developers.google.com/admob)
- [Mobile Ads SDK (Android)](https://developers.google.com/admob/android/quick-start)

`/`

Language

- [English](https://developers.google.com/admob/android/native)
- [Deutsch](https://developers.google.com/admob/android/native?hl=de)
- [Español](https://developers.google.com/admob/android/native?hl=es)
- [Español – América Latina](https://developers.google.com/admob/android/native?hl=es-419)
- [Français](https://developers.google.com/admob/android/native?hl=fr)
- [Indonesia](https://developers.google.com/admob/android/native?hl=id)
- [Italiano](https://developers.google.com/admob/android/native?hl=it)
- [Polski](https://developers.google.com/admob/android/native?hl=pl)
- [Português – Brasil](https://developers.google.com/admob/android/native?hl=pt-br)
- [Tiếng Việt](https://developers.google.com/admob/android/native?hl=vi)
- [Türkçe](https://developers.google.com/admob/android/native?hl=tr)
- [Русский](https://developers.google.com/admob/android/native?hl=ru)
- [עברית](https://developers.google.com/admob/android/native?hl=he)
- [العربيّة](https://developers.google.com/admob/android/native?hl=ar)
- [فارسی](https://developers.google.com/admob/android/native?hl=fa)
- [हिंदी](https://developers.google.com/admob/android/native?hl=hi)
- [বাংলা](https://developers.google.com/admob/android/native?hl=bn)
- [ภาษาไทย](https://developers.google.com/admob/android/native?hl=th)
- [中文 – 简体](https://developers.google.com/admob/android/native?hl=zh-cn)
- [中文 – 繁體](https://developers.google.com/admob/android/native?hl=zh-tw)
- [日本語](https://developers.google.com/admob/android/native?hl=ja)
- [한국어](https://developers.google.com/admob/android/native?hl=ko)

[Sign in](https://developers.google.com/_d/signin?continue=https%3A%2F%2Fdevelopers.google.com%2Fadmob%2Fandroid%2Fnative&prompt=select_account)

- On this page
- [Prerequisites](https://developers.google.com/admob/android/native#prerequisites_2)
- [Always test with test ads](https://developers.google.com/admob/android/native#always_test_with_test_ads_2)
- [Load ads](https://developers.google.com/admob/android/native#load-ad)
  - [Build an AdLoader](https://developers.google.com/admob/android/native#build)
  - [Set up an AdListener with the AdLoader (optional)](https://developers.google.com/admob/android/native#adlistener)
  - [Request ads](https://developers.google.com/admob/android/native#request_ads)
  - [Release resources](https://developers.google.com/admob/android/native#release_resources)
  - [Best practices](https://developers.google.com/admob/android/native#best_practices)
- [Hardware acceleration for video ads](https://developers.google.com/admob/android/native#hardware)
- [Display your ad](https://developers.google.com/admob/android/native#display_your_ad)

- [Home](https://developers.google.com/)
- [Products](https://developers.google.com/products)
- [AdMob](https://developers.google.com/admob)
- [Mobile Ads SDK (Android)](https://developers.google.com/admob/android/quick-start)

Was this helpful?

# Load a native ad    Stay organized with collections      Save and categorize content based on your preferences.

- On this page
- [Prerequisites](https://developers.google.com/admob/android/native#prerequisites_2)
- [Always test with test ads](https://developers.google.com/admob/android/native#always_test_with_test_ads_2)
- [Load ads](https://developers.google.com/admob/android/native#load-ad)
  - [Build an AdLoader](https://developers.google.com/admob/android/native#build)
  - [Set up an AdListener with the AdLoader (optional)](https://developers.google.com/admob/android/native#adlistener)
  - [Request ads](https://developers.google.com/admob/android/native#request_ads)
  - [Release resources](https://developers.google.com/admob/android/native#release_resources)
  - [Best practices](https://developers.google.com/admob/android/native#best_practices)
- [Hardware acceleration for video ads](https://developers.google.com/admob/android/native#hardware)
- [Display your ad](https://developers.google.com/admob/android/native#display_your_ad)

![Spark icon](https://developers.google.com/_static/images/icons/spark.svg)

## Page Summary

outlined\_flag

- Native ads are displayed using UI components native to the platform, matching the app's design.

- When a native ad loads, your app receives an ad object containing assets, and your app is responsible for displaying them, not the Google Mobile Ads SDK.

- Implementing native ads involves loading the ad using the SDK and then displaying the ad content in your app.

- Always test with dedicated test ad unit IDs for native ads to avoid using live ads during development.

- The `AdLoader` class with its `Builder` is used to load native ads, specifying the types of ads the app is ready to receive.

- The `AdListener` can be set up with the `AdLoader` to receive callbacks for ad lifecycle events.

- Use `loadAd()` for a single ad request and `loadAds()` for multiple ad requests, with `loadAds()` working only for Google ads.

- Ensure you call the `destroy()` method on all loaded native ads to release resources and prevent memory leaks.

- Hardware acceleration must be enabled for video ads to show successfully in native ad views.


Select platform: [Android\\
![New](https://developers.google.com/static/admob/images/new.svg)](https://developers.google.com/admob/android/next-gen/native "View this page for the GMA Next-Gen SDK platform docs.") [Android](https://developers.google.com/admob/android/native "View this page for the Android platform docs.") [iOS](https://developers.google.com/admob/ios/native "View this page for the iOS platform docs.") [Flutter](https://developers.google.com/admob/flutter/native "View this page for the Flutter platform docs.") [Unity](https://developers.google.com/admob/unity/native-overlay "View this page for the Unity platform docs.")

Integrate AdMob Native Ads on Android with the Google Mobile Ads SDK - YouTube

Tap to unmute

[Integrate AdMob Native Ads on Android with the Google Mobile Ads SDK](https://www.youtube.com/watch?v=Bta2FE1MFjE) [Google AdMob](https://www.youtube.com/channel/UCLHibHBWfeKWJwqQpJfCEuA)

Google AdMob89.2K subscribers

[Watch on](https://www.youtube.com/watch?v=Bta2FE1MFjE)

![](https://developers.google.com/static/admob/images/format-native.svg)

Native ads are ad assets that are presented to users through UI components that
are native to the platform. They're shown using the same types of views with
which you're already building your layouts, and can be formatted to match your
app's visual design.

When a native ad loads, your app receives an ad object that contains its assets,
and the app—rather than Google Mobile Ads SDK—is then
responsible for displaying them.

Broadly speaking, there are two parts to successfully implementing native ads:
Loading an ad using the SDK and then displaying the ad content in your app.

This page shows how to use the SDK to load

[native ads](https://support.google.com/admob/answer/7187428).

Tip: Learn more about native ads in our [Native Ads\\
Playbook](https://admob.google.com/home/resources/native-ads-playbook/).

You can also check out some customer success stories:
[case study 1](https://admob.google.com/home/resources/alarmmon-achieves-higher-rpm-with-admob-triggered-native-ads/),
[case study 2](https://admob.google.com/home/resources/linghit-limited-doubles-ad-revenue-with-admob-native-ads/).

## Prerequisites

Before you continue, [set up Google Mobile Ads SDK](https://developers.google.com/admob/android/quick-start).

## Always test with test ads

When building and testing your apps, make sure you use test ads rather than
live, production ads.

The easiest way to load test ads is to use our dedicated test ad unit ID
for native ads on Android:

`ca-app-pub-3940256099942544/2247696110`

It's been specially configured to return test ads for every request, and you can
use it in your own apps while coding, testing, and debugging. Just make sure you
replace it with your own ad unit ID before publishing your app.

For details on Google Mobile Ads SDK test ads, see
[Enable test ads](https://developers.google.com/admob/android/test-ads).

## Load ads

Native ads are loaded with the
[`AdLoader`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdLoader) class,
which has its own
[`Builder`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdLoader.Builder)
class to customize it during creation. By adding listeners to the `AdLoader`
when building it, an app specifies which types of native ads it is ready to
receive. The `AdLoader` then requests just those types.

### Build an AdLoader

The following code demonstrates how to build an `AdLoader` that can load native
ads:

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
// It is recommended to call AdLoader.Builder on a background thread.
new Thread(
        () -> {
          AdLoader adLoader =
              new AdLoader.Builder(context, "AD_UNIT_ID")
                  .forNativeAd(
                      new NativeAd.OnNativeAdLoadedListener() {
                        @Override
                        // The native ad loaded successfully. You can show the ad.
                        public void onNativeAdLoaded(@NonNull NativeAd nativeAd) {}
                      })
                  .withAdListener(
                      new AdListener() {
                        @Override
                        // The native ad load failed. Check the adError message for failure
                        // reasons.
                        public void onAdFailedToLoad(@NonNull LoadAdError adError) {}
                      })
                  // Use the NativeAdOptions.Builder class to specify individual options
                  // settings.
                  .withNativeAdOptions(new NativeAdOptions.Builder().build())
                  .build();
        })
    .start();NativeAdSnippets.java
```

```
// It is recommended to call AdLoader.Builder on a background thread.
CoroutineScope(Dispatchers.IO).launch {
  val adLoader =
    AdLoader.Builder(context, "AD_UNIT_ID")
      .forNativeAd { nativeAd ->
        // The native ad loaded successfully. You can show the ad.
      }
      .withAdListener(
        object : AdListener() {
          override fun onAdFailedToLoad(adError: LoadAdError) {
            // The native ad load failed. Check the adError message for failure reasons.
          }
        }
      )
      // Use the NativeAdOptions.Builder class to specify individual options settings.
      .withNativeAdOptions(NativeAdOptions.Builder().build())
      .build()
}NativeAdSnippets.kt
```

Replace AD\_UNIT\_ID with your test device ID.

The
[`forNativeAd()`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdLoader.Builder#forNativeAd(com.google.android.gms.ads.nativead.NativeAd.OnNativeAdLoadedListener))
method is responsible for preparing the `AdLoader` for the `NativeAd` format.
When an ad has loaded successfully, the listener object's `onNativeAdLoaded()`
method is called.

### Set up an AdListener with the AdLoader (optional)

When creating the `AdLoader`, the
[`withAdListener`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdLoader.Builder#withAdListener(com.google.android.gms.ads.AdListener))
function sets an
[`AdListener`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdListener) for the
loader. The method takes an `AdListener` as its lone parameter, which receives
callbacks from the `AdLoader` when ad lifecycle events take place:

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
adLoaderBuilder.withAdListener(
    // Override AdListener callbacks here.
    new AdListener() {});NativeAdSnippets.java
```

```
adLoaderBuilder.withAdListener(
  // Override AdListener callbacks here.
  object : AdListener() {}
)NativeAdSnippets.kt
```

### Request ads

Once you've finished building an `AdLoader`, it's time to use it to request ads.
There are two methods available for this: `loadAd()` and `loadAds()`.

#### `loadAd()`

This method sends a request for a single ad.

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
adLoader.loadAd(new AdRequest.Builder().build());NativeAdSnippets.java
```

```
adLoader.loadAd(AdRequest.Builder().build())NativeAdSnippets.kt
```

#### `loadAds()`

This method sends a request for multiple ads (up to five):

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
// Load three native ads.
adLoader.loadAds(new AdRequest.Builder().build(), 3);NativeAdSnippets.java
```

```
// Load three native ads.
adLoader.loadAds(AdRequest.Builder().build(), 3)NativeAdSnippets.kt
```

Both methods take an
[`AdRequest`](https://developers.google.com/admob/android/reference/com/google/android/gms/ads/AdRequest)
object as their first parameter. This is the same
`AdRequest` class used by banners and interstitials,
and you can use methods of the `AdRequest` class to
add [targeting information](https://developers.google.com/admob/android/targeting), just as
you would with other ad formats.

#### Load multiple ads (optional)

The `loadAds()` method takes an additional parameter: the number of ads the SDK
should attempt to load for the request. This number is capped at five, and it's
not guaranteed that the SDK will return the exact number of ads requested.

Returned Google ads will all be different from each other, though ads from
reserved inventory or third-party buyers are not guaranteed to be unique.

Don't use the `loadAds()` method if you're using mediation, as requests for
multiple native ads don't work for ad unit IDs that have been
configured for mediation.

#### Callbacks

After a call to `loadAd()`, a single callback is made to the previously defined
[listener methods](https://developers.google.com/admob/android/native#build) to deliver the native ad object or report an error.

After a call to `loadAds()`, multiple such callbacks are made (at least one,
and no more than the number of ads requested). Apps requesting multiple ads
should call `AdLoader.isLoading()` in their callback implementations to
determine whether the loading process has finished.

Here's an example showing how to check `isLoading()` in the
`onNativeAdLoaded()` callback:

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
adLoaderBuilder
    .forNativeAd(
        nativeAd -> {
          // This callback is invoked when a native ad is successfully loaded.
        })
    .build();NativeAdSnippets.java
```

```
adLoaderBuilder
  .forNativeAd { nativeAd ->
    // This callback is invoked when a native ad is successfully loaded.
  }
  .build()NativeAdSnippets.kt
```

### Release resources

Be sure to use the `destroy()` method on loaded native ads. This releases
utilized resources and prevents memory leaks.

Verify that all `NativeAd` references are destroyed in your activity's
`onDestroy()` method.

In your `onNativeAdLoaded` callback, make sure to destroy any existing
native ads that will be dereferenced.

Another key check is if the activity is destroyed and if so, call `destroy()` on
the returned ad and return immediately:

[Java](https://developers.google.com/admob/android/native#java)[Kotlin](https://developers.google.com/admob/android/native#kotlin)More

```
nativeAd.destroy();NativeAdSnippets.java
```

```
nativeAd.destroy()NativeAdSnippets.kt
```

### Best practices

Follow these rules when loading ads.

- Apps that use native ads in a list should precache the list of ads.

- When precaching ads, clear your cache and reload after one hour.


- Don't call `loadAd()` or
`loadAds()` on an `AdLoader` until the first
request finishes loading.

- Limit native ad caching to only what is needed. For example when precaching,
only cache the ads that are immediately visible on the screen. Native ads
have a large memory footprint, and caching native ads without destroying them
results in excessive memory use.

- Destroy native ads when no longer in use.


## Hardware acceleration for video ads

In order for video ads to show successfully in your native ad views,
[hardware\\
acceleration](https://developer.android.com/guide/topics/graphics/hardware-accel)
must be enabled.

Hardware acceleration is enabled by default, but some apps may choose to
disable it. If this applies to your app, we recommend enabling hardware
acceleration for Activity classes that use ads.

#### Enabling hardware acceleration

If your app does not behave properly with hardware acceleration turned on
globally, you can control it for individual activities as well. To enable or
disable hardware acceleration, use the `android:hardwareAccelerated` attribute
for the
[`<application>`](https://developer.android.com/guide/topics/manifest/application-element)
and
[`<activity>`](https://developer.android.com/guide/topics/manifest/activity-element)
elements in your `AndroidManifest.xml`. The following example enables hardware
acceleration for the entire app but disables it for one activity:

```
<application android:hardwareAccelerated="true">
    <!-- For activities that use ads, hardwareAcceleration should be true. -->
    <activity android:hardwareAccelerated="true" />
    <!-- For activities that don't use ads, hardwareAcceleration can be false. -->
    <activity android:hardwareAccelerated="false" />
</application>
HardwareAccelerationSnippet.xml
```

See the [HW acceleration\\
guide](https://developer.android.com/guide/topics/graphics/hardware-accel) for
more information about options for controlling hardware acceleration. Note
that individual ad views cannot be enabled for hardware acceleration if the
Activity is disabled, so the Activity itself must have hardware acceleration
enabled.

## Display your ad

Once you have loaded an ad, all that remains is to display it to your users.
Head over to our [Native Advanced\\
guide](https://developers.google.com/admob/android/native/advanced) to see how.

Was this helpful?

Except as otherwise noted, the content of this page is licensed under the [Creative Commons Attribution 4.0 License](https://creativecommons.org/licenses/by/4.0/), and code samples are licensed under the [Apache 2.0 License](https://www.apache.org/licenses/LICENSE-2.0). For details, see the [Google Developers Site Policies](https://developers.google.com/site-policies). Java is a registered trademark of Oracle and/or its affiliates.

Last updated 2026-04-16 UTC.




\[\[\["Easy to understand","easyToUnderstand","thumb-up"\],\["Solved my problem","solvedMyProblem","thumb-up"\],\["Other","otherUp","thumb-up"\]\],\[\["Missing the information I need","missingTheInformationINeed","thumb-down"\],\["Too complicated / too many steps","tooComplicatedTooManySteps","thumb-down"\],\["Out of date","outOfDate","thumb-down"\],\["Samples / code issue","samplesCodeIssue","thumb-down"\],\["Other","otherDown","thumb-down"\]\],\["Last updated 2026-04-16 UTC."\],\[\],\["Native ads are integrated into app layouts using platform-native UI components. Implementation involves loading ads via the \`AdLoader\` class and displaying them within the app. The \`AdLoader\` is built with listeners to specify the types of native ads to receive and handles ad loading. Use \`loadAd()\` for single ads or \`loadAds()\` for up to five. After loading, the ad or an error will be delivered to the listener. Finally, always destroy unused or dereferenced ads using the \`destroy()\` method to prevent memory leaks.\\n"\]\]